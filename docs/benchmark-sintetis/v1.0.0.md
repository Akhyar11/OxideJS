# Benchmark Sintetis `v1.0.0`

## Metadata

- Tanggal: 2026-04-17
- Versi: `1.0.0`
- Commit: `47e7734`
- Tujuan snapshot: baseline awal untuk versi sekarang agar peningkatan performa versi berikutnya punya acuan yang tetap

## Environment

- OS: belum dicatat
- Runtime Node.js: dijalankan via `node -r ts-node/register/transpile-only`
- Backend native aktif: ya, terdeteksi `true` pada benchmark transformer block
- Catatan hardware: belum dicatat

## Daftar Benchmark

| math_synthetic_v1 | `test/benchmark_sintetis_matematika.ts` | `node -r ts-node/register/transpile-only test/benchmark_sintetis_matematika.ts` | passed |

## Hasil

### 1. `training_step`

- Status: passed
- Fokus benchmark: synthetic `forward + backward` untuk training step transformer
- Metrik utama:
  - `iterations`: `20`
  - `msPerBatch`: `265.91340735000006`
  - `batchesPerSec`: `3.7606227153630574`
  - `samplesPerSec`: `240.67985378323567`
- Output ringkas:

```json
{"benchmark":"training_step","iterations":20,"msPerBatch":265.91340735000006,"batchesPerSec":3.7606227153630574,"samplesPerSec":240.67985378323567}
```

### 2. `transformer_block`

- Status: passed
- Fokus benchmark: synthetic forward pass untuk blok transformer
- Konteks output:
  - `Native Backend Available: true`
  - `SeqLen=128`
  - `EmbedDim=256`
- Metrik utama:
  - `Self Attention Forward Avg Time`: `1.548515599999996 ms`
  - `Layer Normalization Forward Avg Time`: `0.2251800000000003 ms`
- Output ringkas:

```text
=== Transformer Block Benchmark ===
Native Backend Available: true
Config: SeqLen=128, EmbedDim=256

--- Self Attention Forward ---
Avg Time: 1.548515599999996 ms

--- Layer Normalization Forward ---
Avg Time: 0.2251800000000003 ms
```

### 3. `math_synthetic_v1`

- Status: passed
- Fokus benchmark: representasi throughput pada dataset matematika (`prompt + response`)
- Konteks output:
  - `VocabPath: project/math-bot/dataset/math_vocab.json`
  - `DatasetPath: dataset/dataset_matematika_1000.json`
- Metrik utama:
  - `samples`: `4210`
  - `msPerSample`: `2.99313497719715`
  - `samplesPerSec`: `334.09786314963526`
- Output ringkas:

```json
{"benchmark":"math_synthetic_v1","samples":4210,"msPerSample":2.99313497719715,"samplesPerSec":334.09786314963526}
```

## Benchmark Gagal

Saat ini tidak ada benchmark yang gagal. `wikipedia_subset` yang sebelumnya gagal telah dihapus dan diganti dengan `math_synthetic_v1` yang lebih stabil.

## Perubahan Penting Dibanding Versi Sebelumnya

- Ini adalah entri baseline pertama, jadi belum ada pembanding versi sebelumnya.

## Interpretasi

- `training_step` saat ini adalah benchmark sintetis paling siap untuk dijadikan acuan utama karena menghasilkan metrik throughput yang langsung bisa dibandingkan antar versi.
- `transformer_block` berguna sebagai benchmark lapisan bawah untuk melihat dampak optimasi pada attention dan layer normalization secara terpisah.
- `math_synthetic_v1` (pengganti `wikipedia_subset`) sekarang menggunakan dataset matematika. Ini memberikan workload yang lebih stabil dan relevan dengan project generative-bot.

## Tindak Lanjut

- Tambahkan metadata environment yang lebih lengkap pada snapshot berikutnya, terutama OS, versi Node.js, dan spesifikasi CPU.
- Pertahankan benchmark `training_step` sebagai baseline utama lintas versi.
- Gunakan `test/benchmark_sintetis_matematika.ts` sebagai benchmark throughput pada workload teks nyata di masa depan.
