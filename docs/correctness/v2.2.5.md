# Correctness Snapshot `v2.2.5`

## Metadata

- Tanggal: 25 April 2026
- Versi: `2.2.5`
- Commit: `ffb55ff` + local patch
- Command suite gabungan: `npm test`
- Status: `pass`

## Cakupan

Snapshot ini mencakup:

- learning-based correctness untuk keluarga recurrent: `RNN`, `LSTM`, `GRU`
- correctness API untuk `Transformers.predictMode`
- learning-based correctness untuk transformer `full-token`
- learning-based correctness untuk transformer `full-token-trimPad`
- learning-based correctness untuk transformer `next-token` legacy path

## Hasil

### 1. Recurrent Learning Correctness

| Family | Initial Loss | Final Loss | Improvement |
| --- | ---: | ---: | ---: |
| `rnn` | `0.316538` | `0.001571` | `0.314967` |
| `lstm` | `0.384432` | `0.000851` | `0.383581` |
| `gru` | `0.372673` | `0.001259` | `0.371414` |

### 2. Transformer API Correctness

| Check | Status |
| --- | --- |
| `predict default next-token` | `pass` |
| `predict full-sequence` | `pass` |
| `set/get predictMode` | `pass` |
| `predict restores training mode` | `pass` |

### 3. Transformer Learning Correctness

| Mode | Initial Loss | Final Loss | Improvement |
| --- | ---: | ---: | ---: |
| `full-token` | `2.667819` | `0.004995` | `2.662824` |
| `full-token-trimPad` | `2.527039` | `0.006869` | `2.520170` |
| `next-token` | `1.265933` | `0.007307` | `1.258626` |

## Catatan Validasi

- Semua correctness suite pada entry point `npm test` berstatus `pass`.
- Setiap suite learning menunjukkan `finalLoss < initialLoss`.
- Transformer API correctness tetap valid setelah patch performa pada validation/loss path.

## Perubahan Penting

- Snapshot ini menjadi companion untuk benchmark sintetis `v2.2.5`.
- Patch `v2.2.5` membekukan hasil correctness setelah optimasi:
  - loss resolution `Sequential` tanpa serialisasi layer output
  - validation batching untuk model non-recurrent
  - loss full-sequence transformer tanpa materialisasi softmax penuh
  - lookup embedding dengan typed buffer dan validasi satu pass
  - tokenizer BPE training/update dengan merge compact in-place

## Risiko Residual

- Snapshot ini belum menyertakan benchmark khusus micro untuk training tokenizer BPE.
- Hasil learning tetap stochastic dan dapat berubah antar run, tetapi kontrak pass/fail suite terpenuhi.

## Versioning

Versi aktif proyek saat ini adalah `2.2.5`.

Proyek ini memakai format versi `MAJOR.MINOR.PATCH` seperti `2.2.5`.

- Angka paling depan (`MAJOR`): perubahan besar yang biasanya membawa breaking change atau perubahan arsitektur utama.
- Angka tengah (`MINOR`): penambahan fitur baru atau peningkatan yang tetap kompatibel dengan versi sebelumnya.
- Angka paling belakang (`PATCH`): perbaikan bug, optimasi kecil, cleanup, atau perubahan minor yang tidak mengubah API utama.
