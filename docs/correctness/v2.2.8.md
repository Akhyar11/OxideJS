# Correctness Snapshot `v2.2.8`

## Metadata

- Tanggal: 27 April 2026
- Versi: `2.2.8`
- Commit: `local-sparse-native`
- Status: `PASS`

## Deskripsi Perubahan
Snapshot ini memverifikasi kebenaran implementasi native sparse untuk semua optimizer dan layer Embedding. Meskipun logika berubah menjadi sparse di backend Rust, hasil pembelajaran (loss reduction) tetap konsisten dengan baseline matematis.

## Cakupan Pengujian

### 1. Optimizer Correctness (Native)
- **Adam**: Berhasil melakukan konvergensi pada model Transformer dengan mode native sparse.
- **SGD/Momentum/NAG/AdaGrad**: Terverifikasi melalui suite optimizer internal yang memastikan state (velocity/sum) diperbarui dengan benar hanya pada indeks yang aktif.

### 2. Embedding Layer
- Integrasi `embedding_backward_sparse_native` menghasilkan gradien yang identik dengan versi JavaScript.
- Tidak ada gradien yang bocor ke token di luar batch.
- Penanganan `pad_token_id` tetap valid (gradien nol untuk padding).

### 3. Suite Gabungan (`npm test`)
Semua tes termasuk:
- `RNN/LSTM/GRU` learning test: **PASS**
- `Transformers` full-sequence learning test: **PASS**
- `BPETokenizer` unicode & multilingual: **PASS**

## Hasil Ringkas Command

```bash
npm test
```

Output:
- `rnn` epoch loss: `0.98 -> 0.69`
- `transformer` epoch loss: `5.01 -> 4.75`
- Semua assertion kebenaran matrix: **OK**

## Interpretasi
Implementasi native sparse dinyatakan **stabil dan benar**. Kecepatan yang didapat dari backend Rust tidak mengorbankan akurasi model.
