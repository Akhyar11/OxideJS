# Ignore Rust source and build artifacts
src-rust/
target/
*.rs
Cargo.toml
Cargo.lock

# Ignore TS source (optional, but good for clean package)
# src/ 

# Keep only what's needed
!dist/
!index.js
!*.node
!package.json
!README.md
!LICENSE
