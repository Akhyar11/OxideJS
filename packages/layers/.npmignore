# Ignore Rust build target directory (large build cache)
target/
src-rust/target/

# Ignore TS source (optional, but good for clean package)
# src/ 

# Keep only what's needed
!dist/
!index.js
!*.node
!package.json
!README.md
!LICENSE
!src-rust/
!Cargo.toml
!Cargo.lock
